/**
 * Ocean breath session: vertical-pan backdrop.
 * All dives (zone and full column) use `surface.svg` as the scrolling background.
 */
export const OCEAN_FULL_COLUMN_BACKDROP_SVG = require('../../assets/ocean-full-column-surface.svg');
